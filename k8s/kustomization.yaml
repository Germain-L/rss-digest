apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - pvc.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - cronjob.yaml

commonLabels:
  app.kubernetes.io/name: rss-digest
  app.kubernetes.io/component: news-aggregator
